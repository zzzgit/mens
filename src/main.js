// two hierachy: memory and persisted 
// three layers: memory, local, remote 
// master-slave mode, master push to and pull from slave, slave just respond to master
// memory is master and local is slave, local is master and remote is slave.
